# פורמטים סטטוטוריים

קבצים שהמדינה מקבלת. פורמט שגוי = דיווח שנדחה.

---

## 1. מנות — רשות האוכלוסין

`src/tik/manot.js` · `src/tik/cp1255.js` · ממשק ב-`#manot`

> ⚠️ **`MANOT.calibrated = false`** — רוחבי השדות **זמניים**. אין לשדר לפני
> כיול מול קובץ דוגמה אמיתי. ראה `NEEDS.md`.

### מבנה הקובץ

שורת נתונים אחת באורך **704 תווים קבוע**, קידוד Windows-1255, ואחריה שורת
כותרת תחתונה קבועה, מופרדות ב-CRLF.

```
'500' + ח.פ בן 9 ספרות + קוד פעולה בן 6 ספרות + [שדות ברוחב קבוע] + '000'
50021609556809990001
```

שם הקובץ: `oz_siud_manot_216095568_NN.txt`

### קודי פעולה

| פעולה | קוד |
|---|---|
| מעבר בין מעסיקים | `090202` |
| מחליף מלשכה אחרת | `090205` |
| מחליף מלשכה אחרת · 51-63 חודש | `090205` |
| הברקה · הזמנת עובד מחו״ל | `090104` |

שתי וריאציות "מחליף" חולקות קוד זהה ונבדלות רק בנוסח מכתב הלוואי.
"הברקה" מוסיפה שדות כתובת מוצא (`foreignAddress: true`).

### רצף השדות

מוגדר בטבלת `SEGMENTS` — 16-19 שדות ברוחב קבוע:

| שדה | רוחב |
|---|---|
| ארץ | 3 |
| דרכון | 15 |
| שם משפחה / פרטי / האב באנגלית | 20 כל אחד |
| שם משפחה בעברית | 22 |
| שם פרטי בעברית | 20 |
| ספרת מין + תאריך לידה YYYYMMDD | 9 |
| בלוק היתר | 24 |
| `'1'` + ת״ז מעסיק | 10 |
| שם משפחה / פרטי של המעסיק | 18 כל אחד |
| ספרת מין + סמל יישוב | 9 |
| יישוב | 20 |
| רחוב | 30 |
| טלפון | 12 |

מין ממופה לספרה מובילה: `1` / `2` / `0`.

**כל טבלת ה-`SEGMENTS` קיימת כדי שנעילת הפורמט תהיה עריכה במקום אחד.**

### קודי ארצות שהוזנו

הודו 110 · פיליפינים 131 · אוזבקיסטן 315 · סרי לנקה 112

### `cp1255.js` — המקודד

מקודד Windows-1255 שנכתב ידנית:

- ASCII עובר כמו שהוא
- `א`..`ת` → `0xE0 + היסט`
- מפת `EXTRA` ל-₪, גרש, גרשיים, מקפים, מרכאות
- **תו לא מוכר → רווח** — כדי שגליף חורג לעולם לא יזיז את הרשת הקבועה

---

## 2. דוח רבעוני למשרד הפנים (301)

`src/tik/interiorReport.js`

### הדוקטרינה: להזריק, לא לבנות מחדש

ה-`.xlsx` הרשמי של המשרד מצורף כנכס קבוע
(`src/tik/assets/interior-quarterly-template.xlsx`, 390KB). הקוד פותח את ה-zip,
משכתב את `xl/worksheets/sheet2.xml` כמחרוזת, ומזריק ערכים ל**שורות הריקות
המעוצבות מראש 9-508** (`ROWS_AVAILABLE = 500`), תוך שימוש חוזר בעיצוב של כל תא.

### טבלת ה-styles — קריטית ולא ניתנת לגילוי

| עמודות | style |
|---|---|
| `A` | `14` |
| `B` / `H` / `M` | `61` (פתיחת קבוצה) |
| `C`-`F` / `I`-`K` / `O`-`Q` | `60` |
| `G` / `L` / `R` | `62` (סגירת קבוצה) |
| `N` (סטטוס) | `63` |

הסגנונות 60-63 **נוספו לתבנית** כווריאציות תואמות-מסגרת הנושאות fill 5 —
ההצללה התכולה `#E0E9F8` שדוחות אמיתיים משתמשים בה.

### 🔑 תאריכים חייבים להיכתב כטקסט

```xml
<c r="..." s="..." t="inlineStr"><is><t xml:space="preserve">31/03/2026</t></is></c>
```

התאים בפורמט "General". מספר סידורי גולמי יוצג כמספר עירום — זה היה הבאג המקורי.

### מיפויים

| סוג ביקור | טקסט בטופס |
|---|---|
| השמה | `טרום השמה` |
| 30 יום | `אחרי` |
| שוטף | `שוטף` |

`PLACEMENT` ממפה 5 ניסוחים של המשרד ל-3 אפשרויות בטופס.
`DEFAULT_ROLE = 'ע.סוציאלי'`. תאי כותרת: C4 סוכנות, C5 שנה, C6 רבעון.

`splitName()` כאן: **שם משפחה ראשון**.

אין חתימה דיגיטלית מוטבעת — המשרד מוריד וחותם ידנית.

---

## 3. דוח גביית אגרה רבעוני (306)

`src/tik/feeReport.js`

אותה טכניקת הזרקה, תבנית אחרת (`fee-quarterly-template.xlsx`).

| פרמטר | ערך |
|---|---|
| שורת נתונים ראשונה | 5 |
| שורות זמינות | 30 (5-34) |
| style | `3` |

| עמודה | תוכן |
|---|---|
| A | מס״ד |
| B | לשכה |
| C | ח.פ |
| D | שם פרטי |
| E | שם משפחה |
| F | דרכון |
| G | סכום |
| H-K | שם בנק / מספר / סניף / חשבון |

⚠️ עמודות H-K הן **חשבון הסוכנות** — זהות בכל שורה. הן מזהות את ההעברה למשרד,
ואינן חשבון העובד/ת.

⚠️ `splitName()` כאן היא **ההפך** מזו של `interiorReport.js`:
`{last: parts[0], first: rest}` — שמות בדרכון הם "שם משפחה שם פרטי".

---

## 4. חשבוניות וקבלות

`src/tik/invoices.js` · `INVOICING.md` (כוונת הציות)

לפי הוראות ניהול פנקסים תשל״ג-1973.

### ארבעה סוגי מסמך

| סוג | סדרה | קוד BKM | סימן |
|---|---|---|---|
| קבלה | `K` | 400 | +1 |
| חשבונית מס | `I` | 305 | +1 |
| חשבונית מס/קבלה | `IR` | 320 | +1 |
| חשבונית זיכוי | `Z` | 330 | **−1** |

פורמט מספר: `<סדרה>-<5 ספרות עם אפסים מובילים>` — למשל `K-00042`.

### אמצעי תשלום

מזומן 1 · המחאה 2 · כרטיס אשראי 3 · העברה בנקאית 4

### אלגוריתם המספור

1. קרא את המספר הגבוה ביותר הקיים לסוג הזה
2. +1
3. הוסף
4. בשגיאת `duplicate|unique` — **קרא מחדש וטפס**, עד **25 ניסיונות**

⚠️ הלולאה חסרת-פערים **רק אם קיים אינדקס ייחודי על `(docType, number)`** —
והוא אינו מוגדר ב-`schema.sql`. ראה `docs/OPEN-ISSUES.md` §8.

### מע״מ

`VAT_RATE = 0.17`. `vatBreakdown()` גוזר את הנטו **אחורה מברוטו כולל מע״מ**,
כי כך מצוטט המחיר ללקוח.

### תנאי מקדים

ח.פ החברה חייב להיות מוגדר בהגדרות המשרד — אחרת ההנפקה זורקת שגיאה.

### כלל ההקפאה

**אין נתיב UPDATE בקוד.** `creditDocument()` הוא התיקון היחיד, והוא כותב אירוע
`credited` ביומן במקום לשנות את המקור.

### יומן ביקורת

`kind: 'taxdoc_event'` · אירועים: `issued` / `printed` / `emailed` / `credited`

---

## 5. ייצוא מבנה אחיד (BKM)

`src/tik/bkmExport.js`

רשומות מופרדות בקו אנכי:

| רשומה | תוכן |
|---|---|
| `A000` | פתיחה: שם תוכנה `OGEN-CRM`, גרסת פורמט `1.31`, ח.פ, תקופה, חותמת זמן |
| `C100` | אחת לכל מסמך |
| `D110` | שורה אחת לכל מסמך |
| `Z900` | ספירות וסכומי בקרה |

סכומים באגורות עם אפסים מובילים · טקסט מרופד ברווחים · תאריכים `YYYYMMDD`.

פלט: `INI.txt` + `BKMVDATA.txt` בקובץ zip.

⚠️ נכתב UTF-8 עם BOM; המפרט מבקש CP1255. מסומן לאישור רו״ח.

---

## 6. ייצוא הנהלת חשבונות (406 / Movein)

`src/tik/accountingExport.js`

מופרד בקו אנכי, שורה לכל מסמך + שורת `Z` מסכמת. סכומים באגורות חתומות.

### שלושה מצבים

| מצב | מסמן כמיוצא? |
|---|---|
| `current` | ✔ |
| `range` | ✘ |
| `full` | ✘ |

### 🔑 הסימון בלי לגעת במסמכים

מסמכי המס קפואים, אז "כבר יוצא" **אינו** שדה עליהם. במקום זה נוצרת שורת אצווה
`kind: 'acct_export'` שמונה את המזהים שכוסו — ו"טרם יוצא" הוא פשוט "לא נמצא
באף אצווה".

### בליעת שגיאות מכוונת

כל כשל DB נזרק מחדש כהודעה עברית גנרית אחת, כדי שקריסת ADODB או מבנה הטבלה
לא יגיעו למשתמש.

---

## 7. קטלוג הדוחות

`src/tik/reports.js` · `src/tik/ReportPage.jsx`

**48 דוחות ממוספרים כדאטה הצהרתית.** כל דוח הוא:

```js
{ no, group, label, desc, params, columns, run }
```

`ReportPage` מריץ כל אחד מהם דרך אותה זרימה פרמטרים → תוצאות. **הוספת דוח היא
הוספת רשומה בטבלה.**

### קבוצות המספור

| טווח | נושא |
|---|---|
| 1-9 | עובדים |
| 101-105 | לקוחות |
| 201-203, 251 | ביקורים |
| 301, 301.1, 301.2, 302-306 | רבעוניים |
| 401-406 | חשבוניות |
| א1-א7 | אישיים |
| מ1-מ4 | משרד |

### מנגנונים

- `COL` — צורות עמודה משותפות
- `dateReport()` — קומבינטור לדוחות מבוססי תאריך
- `byRakaz()` — סינון לפי רכז/ת
- `soon` — דגל לדוח ששמור אך טרם מחושב
- `runReport()` — גוזר אוטומטית רבעון → טווח תאריכים
- `keyOf()` — מפתח מסלול עם קידומת `p`/`o`/`s`/`r`, מזין את `#report/<key>`
