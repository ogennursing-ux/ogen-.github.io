# מסמך לרואה החשבון — מודול קבלות וחשבוניות

**מערכת:** עוגן סיעוד — מערכת ניהול פנימית (פיתוח עצמי, לשימוש החברה בלבד).
**נבנה לפי:** הוראות ניהול פנקסי חשבונות, תשל"ג-1973.
**מטרת המסמך:** לפרט מה נבנה במודול הקבלות והחשבוניות, כדי שרו״ח יוכל לאשר
לפני שימוש בהפקת מסמכים אמיתיים.

> ⚠️ המערכת היא כלי. **האישור החוקי לשימוש בפועל הוא של רואה החשבון.**
> אין להפיק מסמכים אמיתיים לפני אישורו.

---

## איפה זה נמצא
מערכת הרישום ← דוחות ← דוחות חשבוניות (401–405), או ישירות בכתובת `#invoices`.

## מה נבנה — ומה זה עונה בחוק

### ✅ 1. סוגי מסמכים
קבלה · חשבונית מס · חשבונית מס/קבלה · חשבונית זיכוי. לכל סוג קוד מסמך
לפי מבנה אחיד (400 / 305 / 320 / 330).

### ✅ 2. מספור רציף ואוטומטי
מונה נפרד לכל סוג מסמך (K-00001, I-00001, Z-00001…). לפני כל הקצאה המערכת
בודקת את המספר הגבוה הקיים ומקצה את הבא. אם מספר כבר נתפס — מדלגת קדימה,
כך שאין כפילויות ואין דילוגים.
> **המלצה לרו״ח / להקשחה:** להוסיף במסד הנתונים אינדקס ייחודי על
> (סוג מסמך, מספר) — זה הופך את מניעת הכפילות לוודאית ברמת ה-DB גם בעומס.

### ✅ 3. נעילת מסמך (Immutability)
לאחר הפקה המסמך **קפוא**. באפליקציה **אין** כפתור עריכה או מחיקה למסמך שהופק.
תיקון טעות נעשה **אך ורק** בהפקת חשבונית זיכוי שמפנה למסמך המקורי.
> **המלצה לרו״ח / להקשחה:** לבטל הרשאות UPDATE/DELETE על טבלת המסמכים
> (`kind = 'taxdoc'`) ברמת מסד הנתונים (Supabase RLS), כך שגם מפתח לא יוכל
> לשנות מסמך חתום. זו הגדרה שנשלים לפי הנחיית רו״ח.

### ✅ 4. יומן אירועים (Audit Log)
כל אירוע נשמר כרשומה נפרדת בלתי-ניתנת-לשינוי: הפקה, הדפסה, שליחה במייל,
זיכוי — עם מי ביצע ומתי. נצפה בכל מסמך תחת "יומן אירועים".

### ✅ 5. שדות חובה בקבלה
שם החברה, ח.פ, כתובת, תאריך, מספר סידורי, שם הלקוח, פירוט, סכום לפני מע״מ,
מע״מ 17% וסה״כ, אמצעי תשלום. (ח.פ החברה נדרש לפני הפקת המסמך הראשון.)

### ✅ 6. ייצוא קובץ במבנה אחיד (BKM / "קובץ אחיד")
כפתור "ייצוא קובץ אחיד (BKM)" מפיק את שני הקבצים שהמפקח מבקש:
`INI.txt` (אינדקס) ו-`BKMVDATA.txt` (נתונים), במבנה רשומות באורך קבוע:
A000 (פתיחה) · C100 (מסמך) · D110 (שורה) · Z900 (סגירה עם סכומי ביקורת).
> **חובה לאימות עם רו״ח:** מיקומי השדות המדויקים במבנה האחיד צריכים להיבדק
> מול המפרט הרשמי העדכני של רשות המסים לפני הסתמכות בביקורת. המבנה בנוי
> נכון ומפיק קובץ תקין וקריא; הדיוק ברמת השדה טעון אישור.

### ✅ 7. כיתוב "מסמך ממוחשב – נחתם דיגיטלית"
מופיע על כל מסמך, כנדרש למסמך שנשלח דיגיטלית.

---

## מה עדיין דורש השלמה חיצונית (לא נבנה — צריך החלטה/מפתחות)

### 🔑 חתימה אלקטרונית מאושרת
המנגנון בנוי כך שהחתימה תהיה **פעם אחת לתמיד** — תעודת חתימה אחת של החברה
שחותמת אוטומטית על כל מסמך. **צריך:** להשיג תעודת חתימה מחברה מורשית
(למשל קומסיין) ולחבר אותה. עד אז המסמך נושא את הכיתוב אך אינו חתום
קריפטוגרפית. **את התעודה על שם החברה רק הלקוח יכול להשיג.**

### 🔑 API "חשבוניות ישראל" (מספר הקצאה)
חשבונית מס מעל סף החוק (₪20,000 ב-2025, יורד בהדרגה) חייבת לקבל מספר
הקצאה מרשות המסים בזמן אמת לפני מסירה. נדרש רישום מול רשות המסים ומפתחות
API על שם החברה. **הוחלט לדחות לעתיד** — נחבר כשיהיו המפתחות. עד אז
המערכת מיועדת בעיקר לקבלות.

---

## סיכום לרו״ח
המכניקה הנדרשת (מספור, נעילה, יומן, שדות חובה, קובץ אחיד, כיתוב) — בנויה.
לפני הפעלה מלאה נדרשים ממך שלושה אישורים/השלמות:
1. אישור מבנה השדות של הקובץ האחיד מול המפרט הרשמי.
2. הגדרת נעילת המסמכים ברמת מסד הנתונים (נשלים לפי הנחייתך).
3. תעודת חתימה אלקטרונית (ומאוחר יותר — API חשבוניות ישראל).
