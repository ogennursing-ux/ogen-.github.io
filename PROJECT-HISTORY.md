# יומן סבב עבודה — אוגוסט 2026

היקף: שלוש תוספות למערכת המשרד ומיתוג מחדש. **זה אינו תיעוד המערכת** — לתיעוד
ראה [`CLAUDE.md`](CLAUDE.md) ותיקיית [`docs/`](docs/).

> **הערת תיקון (2026-08-13)**
> הגרסה הקודמת של המסמך הזה הכילה טענות בדיקה שלא היו מבוססות על כלום —
> "Playwright Tests (10 runs clean)", "100 בדיקות", "נבדק ב-Chrome/Firefox/Safari".
> **אין ומעולם לא הייתה תשתית בדיקות במאגר הזה.** הטענות הוסרו, וכך גם נתיבי
> קבצים שגויים, משתני סביבה שלא קיימים, וטענות על מאגר שני שלא קיים בסביבה.
> הרשימה המלאה של מה שתוקן: [`docs/OPEN-ISSUES.md`](docs/OPEN-ISSUES.md).

---

## מה נבנה

### 1. בדיקת תקינות נתונים — `#quality`

**קומיט**: `c4bdc39`

- `src/tik/dataQuality.js` — `computeIssues()` ו-`validIsraeliId()`
- `src/tik/QualityDesk.jsx` — ממשק, מקבץ לפי `critical` / `warning`

סריקה **מבוססת חוקים, לא AI** (כך כתוב בכותרת הקובץ — כדי שדגל יהיה תמיד אמין):
ת״ז מעסיק לא תקינה, תאריכי השמה הפוכים, גיל בלתי אפשרי, דרכונים כפולים, ושדות
חיוניים חסרים בהשמות פעילות בלבד.

פירוט הכללים: [`docs/BUSINESS-RULES.md`](docs/BUSINESS-RULES.md) §5.

⚠️ **הקומיט הזה גרם לרגרסיה**: הוא מחק 278 שורות מ-`src/index.css` והוסיף 53
(נטו −225). ראה [`docs/OPEN-ISSUES.md`](docs/OPEN-ISSUES.md) §2.

### 2. הכתבה קולית

**קומיט**: `198bce6`

- `src/tik/MicButton.jsx` — כפתור 🎤 מעל Web Speech API בעברית
- שולב ב-`src/tik/AssistantDesk.jsx` וב-`src/tik/AiDraftModal.jsx`

מחזיר `null` בדפדפן שאינו תומך, כך שהכפתור פשוט לא מופיע.

⚠️ ההערה בקובץ מציינת "works on Chrome over HTTPS". Web Speech API **אינו
ממומש ב-Firefox**.

### 3. תיוק מסמכים אוטומטי — `#autofile`

**קומיט**: `8e2913d`

- `src/tik/autoFile.js` — `detectDocType()`, `expiryOf()`, `analyzeDocument()`, `fileDocument()`
- `src/tik/AutoFileDesk.jsx` — בורר קבצים + כרטיסי בחינה הניתנים לעריכה

הזרימה: בחירת תמונה → `extractDocument()` (מוגדרת ב-`src/tik/gemini.js`, לא כאן)
→ זיהוי סוג מסמך משדות התוקף → התאמת עובד לפי דרכון מנורמל → העלאה ורישום.

`<input accept="image/*">` — PDF פשוט אינו מוצע בבורר, ואין הודעה על כך.

### 4. מיתוג מחדש — "ניהול בקליק"

**קומיטים**: `6f6507c`, `da9a0b9`, `eeec040`

- `src/tik/BrandBar.jsx` — סרגל עליון דביק. `.brandbar` ב-`index.css:3254`,
  לוגו בגובה 66px (48px מתחת ל-600px רוחב)
- `src/tik/RegistryApp.jsx` — הלוגו החליף את הטקסט "מערכת הרישום" בכותרת
  המרשם. `.rg-logo` ב-`index.css:2092`, תמונה בגובה 48px
- `src/tik-main.jsx` — `Shell` מדלג על `BrandBar` ב-`#chat` וב-`#registry`
- `public/nihul-belick.png` — נגזר מ-1536×358 ל-**1230×358** כדי שייקרא בגודל קטן

⚠️ `src/main.jsx` (אפליקציית החתימה) לא נגע בשינוי הזה כלל.

---

## בעיות שתוקנו במהלך העבודה

| בעיה | פתרון |
|---|---|
| `extractDocument()` מחזיר `{patch, raw}` ולא שדות שטוחים | `const fields = { ...(res.raw \|\| {}), ...(res.patch \|\| {}) }` — `autoFile.js:25` |
| הלוגו נראה ככתם כחול בגודל קטן | גזירה מ-1536×1024 ל-1230×358 |
| כפילות מיתוג במרשם | דילוג על `BrandBar` כש-`at === 'registry'` |

---

## מצב הקומיטים

ששת קומיטי הפיצ'רים (`c4bdc39` … `eeec040`) **כבר על `main`**.
הענף `claude/new-document-system-ru1jp6` נושא רק את קומיטי התיעוד.

⚠️ **`main` מכיל קומיט שחסר בענף הזה** — `a96e047` (תיקון תקיעת החתימה).
לפני מיזוג יש לבצע rebase. ראה `docs/OPEN-ISSUES.md` §3ב.

סה״כ: 91 קומיטים ב-HEAD, 88 ב-`main`.

---

## מצב פריסה

הפריסה האחרונה שהצליחה היא `eeec040` (2026-08-06 07:02). **שתי פריסות אחריה
נכשלו**, כולל תיקון לתקיעה בחתימה.

כלומר: שלוש התוספות והמיתוג **כן** באוויר; מה שנעשה אחריהן — לא.

פירוט: [`docs/OPEN-ISSUES.md`](docs/OPEN-ISSUES.md) §1.

---

## מה לא נבדק

אין תשתית בדיקות במאגר. האימות שבוצע בסבב הזה היה `npm run build` בלבד.
לא בוצעה בדיקה בפרודקשן, לא נבדקה תאימות דפדפנים, ולא נבדקה נגישות.
